{
  "name": "javascript-blueprint-build",
  "private": true,
  "type": "module",
  "description": "Build tooling for The JavaScript Blueprint. Not published.",
  "comment": "These were installed with --no-save and no package.json, so the next --no-save install pruned whatever was not asked for - which is how marked disappeared mid-session and the build stopped dead. Listing them fixes that: npm install restores both.",
  "dependencies": {
    "marked": "^12.0.0",
    "pagedjs": "^0.4.3",
    "puppeteer-core": "^25.8.0"
  }
}
